# Dependências para desenvolvimento e testes locais (CPU-only por padrão).
# Evita o download do stack CUDA da NVIDIA (~3-4 GB), desnecessário no Raspberry Pi e nos runners de CI.
# Em produção no container, o Dockerfile.api instala PyTorch CPU seguido de app/requirements.txt.

--extra-index-url https://download.pytorch.org/whl/cpu
torch
torchvision

-r app/requirements.txt

pytest
pytest-cov
pytest-mock
ruff
